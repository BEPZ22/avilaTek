import * as GenericError from './genericError';
import {
  Either,
  Result,
  right,
  wrong,
} from './Result';
import { UseCaseError } from './UseCaseError';
import { Validator } from './/Validator';

export { Either, Result, right, wrong, GenericError, UseCaseError, Validator };
